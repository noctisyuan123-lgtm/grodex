# G3j — UI loosen + Grodex branding

Wave **G3j** polishes `@grodex/web` spacing and user-facing product name.

## Changes

- **Customize:** all sections are controlled accordions (default collapsed); **Expand all** / **Collapse all** in the panel header.
- **Spacing:** looser customize body, MCP cards, sidebar buttons, composer, and session rows.
- **Brand:** user-visible copy uses **Grodex** (capital G). Package ids and `localStorage` keys stay `grodex`.

## Verify

```sh
npm run build -w @grodex/web
```
